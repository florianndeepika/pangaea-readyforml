services:
  - type: web
    name: pangaea-readyforml
    runtime: python
    buildCommand: pip install -e .
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: SAIA_API_KEY
        sync: false
